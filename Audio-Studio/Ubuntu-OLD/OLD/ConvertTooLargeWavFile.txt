How to convert a .wav file larger then 4GB
==========================================

Import in Audacity as raw audio
- set offset to 44 byte (length of wav header)
- set sampling freqeuncy (same as recording)
- set bitrate (same as recording)
- set channels (stereo)
Import
! takes along time
